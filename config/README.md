# Config Layout

- `ratings/positions/` contains per-position scoring configs used by match rating,
  season compute, and calibration.
- `profiles/` contains interpretation configs for role fit and style traits.
- `metadata/` contains descriptive model metadata used for documentation or UI
  surfaces.
